# dotfiles

Bootstrap this setup on another machine with:

```bash
curl -fsSL https://raw.githubusercontent.com/kalleli/dotfiles/main/.local/bin/bootstrap-dotfiles | DOTFILES_REMOTE_URL=https://github.com/kalleli/dotfiles.git bash
```
